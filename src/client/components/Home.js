import React from 'react';

const Home = () => {
    return (
        <div>
            <div>Home New New New Page</div>
            <button onClick={() => console.log("hi")}>Click me</button>
        </div>
    )
};

export default Home;